#include <bits/stdc++.h>
using namespace std;

int fomulas(int num, int size){
  if(size == 0){
    return num;
  }
  else{
    size -= 1;
    for(int i=0; i<size; i++){
      
    }
  }
}

int main() {
  string S;
  cin >> S;
  vector<int> num(S.size());
  cout << stoi(S);
}