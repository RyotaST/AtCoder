#include <bits/stdc++.h>
using namespace std;
#define rep(i,x,n) for (int i=(int)x;i<(int)n;i++)

int main(){
  long long A,L,R;
  long long Left, Right, ans;
  int M;
  cin >> A >> M >> L >> R;
  Left = abs(L-M);
  Right = abs(R-M);
}