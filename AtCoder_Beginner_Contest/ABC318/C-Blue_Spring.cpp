#include <bits/stdc++.h>
using namespace std;
#define rep(i,x,n) for (int i=(int)x;i<(int)n;i++)

int main(){
  int N,D;
  long long P;
  cin >> N >> D >> P;
  vector<long long> F(N);
  long long day = 0;
  vector<long long> dp;
  rep(i,0,N) cin >> F[i];
  
}